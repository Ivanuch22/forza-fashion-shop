query CollectionList($first1: Int = 100) {
	collections(first: $first1, channel: "default-channel") {
		edges {
			node {
				backgroundImage {
					alt
					url
				}
				id
				name
				slug
			}
			cursor
		}
	}
}
