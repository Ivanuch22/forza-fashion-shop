query CollectionList($first1: Int = 100, $channel: String = "default-channel") {
	collections(first: $first1, channel: $channel) {
		edges {
			node {
				backgroundImage {
					alt
					url
				}
				id
				name
				slug
			}
			cursor
		}
	}
}
