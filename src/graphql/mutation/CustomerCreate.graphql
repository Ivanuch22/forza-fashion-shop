mutation CustomerCreate($input: UserCreateInput = {}) {
  customerCreate(input: $input) {
    user {
      email
      id
    }
    errors {
      addressType
      code
      field
      message
    }
  }
}