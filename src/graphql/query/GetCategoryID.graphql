query GetCategoryID($slug: String = "") {
  category(slug: $slug) {
    id
    name
    description
    seoDescription
    seoTitle
    slug
    products(channel: "default-channel") {
      totalCount
    }
  }
}