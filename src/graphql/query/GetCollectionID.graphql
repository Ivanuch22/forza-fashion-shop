query GetCollectionID($slug: String = "", $channel: String = "default-channel") {
	collection(slug: $slug, channel: $channel) {
		id
		name
		description
		seoDescription
		seoTitle
		slug
		products {
			totalCount
		}
	}
}
